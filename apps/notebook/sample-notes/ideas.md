# Ideas

Random sparks worth keeping.

## Product

- A notebook where **agents and humans** edit the same files
- Live cursors driven by `resources/updated`

## Code

```js
const note = await read("ideas.md");
console.log(note.length);
```

Search for the word **agent** to find every note that mentions one.
