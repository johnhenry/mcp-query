// Injected by Vite/Vitest `define` (see vite.config.ts). Absolute path to sample-notes/,
// which the browser hands to the filesystem MCP server via the dev proxy.
declare const __SAMPLE_NOTES_DIR__: string;
