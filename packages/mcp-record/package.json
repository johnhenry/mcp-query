{
  "name": "@mcp-query/record",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Record real MCP server traffic to a cassette and replay it offline as a deterministic mock — VCR/Polly/nock for MCP. Built on mcp-query's transport seam.",
  "bin": {
    "mcp-record": "./src/cli.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "start": "tsx src/cli.ts"
  },
  "dependencies": {
    "mcp-query": "*",
    "@modelcontextprotocol/sdk": "^1.29.0"
  },
  "devDependencies": {
    "tsx": "^4.22.4",
    "typescript": "^5.9.3",
    "vitest": "^4.1.9"
  }
}
